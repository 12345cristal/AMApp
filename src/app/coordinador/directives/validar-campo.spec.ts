import { ValidarCampo } from './validar-campo';

describe('ValidarCampo', () => {
  it('should create an instance', () => {
    const directive = new ValidarCampo();
    expect(directive).toBeTruthy();
  });
});
